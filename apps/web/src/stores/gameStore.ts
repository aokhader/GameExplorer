// Moved to @gameexplorer/client (shared web + mobile). Re-exported here so the
// existing `@/stores/gameStore` import paths keep working.
export { useGameStore } from '@gameexplorer/client';
export type { AnyGameState } from '@gameexplorer/client';
