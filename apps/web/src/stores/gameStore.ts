// Moved to @finesse/client (shared web + mobile). Re-exported here so the
// existing `@/stores/gameStore` import paths keep working.
export { useGameStore } from '@finesse/client';
export type { AnyGameState } from '@finesse/client';
